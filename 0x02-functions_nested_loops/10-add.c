#include "main.h"

/**
 * add - adds up two integer numbers and returns the result
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: a + b
 */
int add(int a, int b)
{

	return (a + b);
}
