#include <main.h>

/**
 * main -  prints the alphabet, in lowercase
 *
 * Return: Always 0
 */

int main(void)
{
	char count;

	count = a;

	while (count <= z)
		_putchar(count);

	_putchar('\n');

	return (0);
}
