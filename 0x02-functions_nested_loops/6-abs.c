#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: the int to be computed.
 * Return: Always 0
 */

int _abs(int)
{
	if (int > 0)
		return (int);
	else
		return (-int);
}
